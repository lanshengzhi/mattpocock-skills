---
"mattpocock-skills": patch
---

to-spec: add a simplicity pass to the process. Before the spec is written, every user story must trace back to the Problem Statement; a story that doesn't is demoted into Out of Scope with one line on why it isn't being built (never silently deleted, and anything the user explicitly asked for stays), and surviving stories are checked against existing modules and already-installed dependencies so reuse is named in Implementation Decisions instead of speccing new work. The pass is synthesis from the conversation and the codebase, not a new round of questions, so the skill's no-interview constraint is unchanged.
