---
"mattpocock-skills": patch
---

Add an architecture **existence test** to `minimal-code` and use it to filter `improve-codebase-architecture` candidates before interface design begins.

- Each candidate now names current evidence, the earliest structural move that holds, and what becomes smaller or disappears.
- A new module comes after leaving, removing, collapsing, reusing, and deepening existing structure. A new polymorphic seam requires two real adapters.
- Architecture scans can now report **No actionable deepening opportunities** instead of filling the report with unsupported candidates.
- Re-sync the skill docs, engineering indexes, and `ask-matt` routing around the new relationship between `minimal-code` and `codebase-design`.
